# BookStore-Web-Server
A College assignment consisting of PHP code, Basic HTML and a SQL database. 
To run it , you would need to download XAMPP and start the Apache and MySQL modules.
Then extract the .sql file to a MySQL database, place the .php code and everything else in Apache's etc/htdocs folder to be used as a web server
